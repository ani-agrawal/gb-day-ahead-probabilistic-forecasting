# Processed data placeholder

`modelling/data/prepare_features.py` writes
`model_table_hourly_epex.parquet` here after the required raw inputs, including
the licensed target, have been supplied locally.
