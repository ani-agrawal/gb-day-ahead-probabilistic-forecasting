# Local publication-model snapshot

This directory is intentionally empty in the public repository. Populate it by
running `python pipeline/prepare_publication_snapshot.py` after the constituent
model pipeline has completed. Its contents are ignored because they contain the
licensed realised price target.
